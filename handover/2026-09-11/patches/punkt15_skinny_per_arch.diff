--- /tmp/claude-1000/-home-mp-Projekte-AIfred-Intelligence/cea48d0f-026d-43a2-924f-87df9414e8a1/scratchpad/p15/marlin.py.orig	2026-09-10 21:07:03.675425647 +0200
+++ /tmp/claude-1000/-home-mp-Projekte-AIfred-Intelligence/cea48d0f-026d-43a2-924f-87df9414e8a1/scratchpad/p15/marlin.py	2026-09-10 21:07:19.811896338 +0200
@@ -178,14 +178,22 @@
         from torch.utils.cpp_extension import load
 
         os.environ.setdefault("TORCH_CUDA_ARCH_LIST", "7.0")
+        # Build for the architecture this worker computes on. A fixed sm_70
+        # cubin also runs on Turing (same major version), but compiled for
+        # sm_75 the kernels are scheduled for Turing (measured 1-6 % at
+        # M <= 4 on the RTX 8000). The ninja cache is keyed by the extension
+        # name alone, so each architecture needs its own name.
+        major, minor = torch.cuda.get_device_capability(torch.cuda.current_device())
+        arch = f"{major}{minor}"
         _skinny_ext = load(
-            name="skinny_nvfp4_v11",
+            name=f"skinny_nvfp4_v11_sm{arch}",
             sources=[_SKINNY_SRC],
             extra_cuda_cflags=["-O3", "--use_fast_math", "-lineinfo",
-                               "-gencode=arch=compute_70,code=sm_70"],
+                               f"-gencode=arch=compute_{arch},code=sm_{arch}"],
             verbose=False,
         )
-        logger.info_once("SM70 skinny NVFP4 kernel loaded from %s", _SKINNY_SRC)
+        logger.info_once("Skinny NVFP4 kernel for sm_%s loaded from %s", arch,
+                         _SKINNY_SRC)
     return _skinny_ext
 
 
