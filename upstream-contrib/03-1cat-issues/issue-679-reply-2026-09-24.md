# Entwurf: Kurzantwort an chenmacnica in 1Cat #679 (24.09.2026)

Status: GESENDET 24.09.2026, https://github.com/1CatAI/1Cat-vLLM/issues/679#issuecomment-5809082260

---

Glad the direction helped, and thanks for the numbers. Both questions are outside what I can speak for, but I can point you to where they live.

1. The TP8 load error comes from 1Cat main, not from my fork: the fused `qkv_proj` branch in `vllm/model_executor/models/mimo_v2.py` splits the whole block with `chunk(tp_size)` and ignores KV-head replication. With 4 KV heads on 8 ranks each rank needs one whole KV head, which is exactly the gap between your 1,696 and 1,856 rows. The MiMo loader is the maintainers' code, so that is a question for them.

2. `VLLM_MIMO_SM70_EXPERIMENT` is not in my fork or in 1Cat main; it must have come with your MiMo patches, so only their author can say why the 3D path was switched off there.

AI assistance was used for this reply; I checked the line in 1Cat main myself.
